{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "g++ build",
            "type": "shell",
            "command": "g++ main.cpp -o main.exe -std=c++11 -I${workspaceFolder}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}